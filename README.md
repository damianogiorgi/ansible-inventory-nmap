# Scans a network and generates and ansible file


Modified from https://github.com/gaberger/hostscan

Install requirements 

pip install requirements.txt

run it

./nmap-scan.py network/subnet -A >>inventoryfile
